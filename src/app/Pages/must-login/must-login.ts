import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-must-login',
  imports: [RouterLink],
  templateUrl: './must-login.html',
  styleUrl: './must-login.scss'
})
export class MustLogin {

}
